ultralytics>=8.3
opencv-python>=4.9
paho-mqtt>=2.0
numpy>=1.26
scipy>=1.12
aiohttp>=3.9
pydantic>=2.0
psutil>=5.9
httpx>=0.27
# Used by VehicleZoneAnalytics to persist per-track zone-entry timestamps
# across edge restarts so overstay alerts show real dwell rather than
# resetting to max_dwell_time_s after every restart.
redis>=5.0
# Optional — UIRenderer falls back to cv2.imencode if absent.
PyTurboJPEG>=1.7
